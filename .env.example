# Supabase (scraper + sync)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here

# Proxy (optional, used by scraper)
#PROXY_URL=http://user:pass@proxy.example.com:8080

# Contact extraction via Supabase/Firecrawl (pipeline flags)
# LIMIT_CONTACTS=50            # optional default limit for contacts step
# CONTACT_LOG_LEVEL=INFO       # log level for contact runner
# DRY_RUN_CONTACTS=false       # set true to avoid persisting contact results
# SKIP_CONTACTS=false          # set true to skip contact step entirely
